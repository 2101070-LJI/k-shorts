# Fonts

여기에 자막 렌더링에 쓸 한글 폰트를 배치.

- `Pretendard-Medium.otf` — 기본 (Clean, Split 템플릿에서 사용)
- `NanumMyeongjo-Regular.ttf` — 감성 (Soft 템플릿)
- `NanumSquare_acB.ttf` — Bold (Bold 템플릿)

런타임에는 `fonts/...` 상대 경로로 참조 (JSON 템플릿에 정의됨).

라이선스 이유로 폰트 파일 자체는 git-ignore 대신 수동 배치를 권장.
